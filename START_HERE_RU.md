# 🚀 НАЧНИТЕ ЗДЕСЬ

## Текущее состояние проекта

✅ Проект TrendScout успешно склонирован с оригинального репозитория
✅ Все файлы настроены и готовы к использованию
✅ База данных настроена (DATABASE_URL обновлен)
✅ Старые файлы удалены

## ⚠️ ВАЖНО: Единственный оставшийся шаг

**Создайте таблицы в базе данных Supabase!**

Из-за блокировки IPv6 на вашей системе, прямое подключение к PostgreSQL не работает.
Нужно создать таблицы через веб-интерфейс Supabase.

### Как это сделать (займет 2 минуты):

1. **Откройте Supabase SQL Editor:**
   ```
   https://supabase.com/dashboard/project/swgkdyhyzvfzuiqwbbtp/sql/new
   ```

2. **Откройте файл `schema.sql`** (находится в корне проекта)

3. **Скопируйте ВСЁ содержимое** файла `schema.sql`

4. **Вставьте в SQL Editor** на сайте Supabase

5. **Нажмите RUN** (или Ctrl+Enter)

6. **Дождитесь сообщения** "Success. No rows returned"

**Готово!** Теперь база данных полностью настроена.

---

## 🏃 Быстрый старт

После создания таблиц в Supabase:

### Вариант 1: Запуск через Docker (Проще)
```bash
docker-compose up
```

### Вариант 2: Запуск по отдельности

**1. Backend:**
```bash
cd server
pip install -r requirements.txt
uvicorn app.main:app --reload
```

**2. Frontend (в другом терминале):**
```bash
cd client
npm install --legacy-peer-deps
npm run dev
```

**3. ML Service (опционально, в третьем терминале):**
```bash
cd ml-service
pip install -r requirements.txt
uvicorn app.main:app --reload --port 8001
```

---

## 📱 Адреса после запуска

- **Frontend:** http://localhost:5173
- **Backend API:** http://localhost:8000/docs
- **ML Service:** http://localhost:8001/docs

---

## 📋 Структура проекта

```
c:\Users\PC\Desktop\89\
│
├── client/                      # React фронтенд (оригинальный дизайн)
│   ├── src/                    # Исходный код
│   ├── package.json            # npm зависимости
│   └── vite.config.ts          # Vite конфигурация
│
├── server/                      # FastAPI бэкенд
│   ├── app/                    # Код приложения
│   │   ├── main.py            # Точка входа
│   │   ├── api/               # API роуты
│   │   ├── db/models.py       # Модели базы данных
│   │   └── services/          # Бизнес-логика
│   ├── requirements.txt        # Python зависимости
│   └── .env                    # Переменные окружения
│
├── ml-service/                  # ML/AI сервис (опционально)
│   ├── app/                    # Код сервиса
│   └── requirements.txt        # Python зависимости
│
├── schema.sql                   # ⭐ SQL схема для Supabase
├── .env                         # Главный файл окружения
├── START_HERE_RU.md            # ⭐ Этот файл (начните здесь!)
├── SETUP_INSTRUCTIONS_RU.md    # Подробная инструкция
├── DATABASE_SETUP.md           # Информация о базе данных
└── docker-compose.yml          # Docker конфигурация
```

---

## 🔑 Настройки в .env файлах

Все уже настроено! Текущие настройки:

### База данных (Supabase):
- **Проект:** `swgkdyhyzvfzuiqwbbtp`
- **Регион:** `aws-0-eu-central-1`
- **Connection Pooling:** включен (порт 6543)

### API Ключи:
- ✅ ANTHROPIC_API_KEY - настроен
- ✅ APIFY_API_TOKEN - настроен
- ✅ GOOGLE_CLIENT_ID - настроен
- ✅ SECRET_KEY - настроен

---

## ❓ Частые вопросы

### "Connection timeout" при запуске
**Решение:** Вы забыли создать таблицы в Supabase. См. раздел "ВАЖНО" выше.

### Ошибки npm при установке
**Решение:**
```bash
npm install --legacy-peer-deps --force
```

### Нужен ли Redis?
Нет, Redis опционален. Приложение будет работать без него.

### Нужен ли ML service?
Нет, он опционален. Backend может работать без ML service.

---

## 🎯 Что дальше?

1. ✅ Создайте таблицы в Supabase (см. выше)
2. ✅ Запустите приложение
3. ✅ Откройте http://localhost:5173
4. ✅ Зарегистрируйтесь и протестируйте!

---

## 📖 Дополнительная информация

- **SETUP_INSTRUCTIONS_RU.md** - детальная инструкция по установке
- **DATABASE_SETUP.md** - информация о базе данных
- **DEVELOPMENT.md** - для разработчиков
- **PROJECT_SUMMARY.md** - обзор проекта

---

## 💡 Подсказка

Если хотите проверить, созданы ли таблицы в Supabase:

1. Зайдите на: https://supabase.com/dashboard/project/swgkdyhyzvfzuiqwbbtp/editor
2. В левом меню должны появиться таблицы: `users`, `trends`, `competitors`, и т.д.

Если таблиц нет - выполните инструкцию из раздела "ВАЖНО" выше.

---

**Удачи!** 🎉

Если возникнут вопросы - все подробно описано в `SETUP_INSTRUCTIONS_RU.md`
