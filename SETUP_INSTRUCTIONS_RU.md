# Инструкция по запуску Risko.ai

## Шаг 1: Настройка базы данных (ОБЯЗАТЕЛЬНО!)

Из-за блокировки IPv6 нужно создать таблицы через веб-интерфейс Supabase:

### Вариант A: Автоматически через SQL Editor (Рекомендуется)

1. Откройте: https://supabase.com/dashboard/project/swgkdyhyzvfzuiqwbbtp/sql/new

2. Откройте файл `schema.sql` из корня проекта

3. Скопируйте ВСЁ содержимое файла `schema.sql`

4. Вставьте в SQL Editor в Supabase

5. Нажмите **RUN** (или Ctrl+Enter)

6. Дождитесь сообщения "Success. No rows returned"

### Вариант B: Через Alembic миграции

Если у вас есть VPN с поддержкой IPv6:

```bash
cd server
pip install alembic
alembic upgrade head
```

## Шаг 2: Проверка подключения

После создания таблиц проверьте подключение:

```bash
cd server
python -c "from app.core.database import engine; print('Connected!')"
```

## Шаг 3: Установка зависимостей

### Backend
```bash
cd server
pip install -r requirements.txt
```

### Frontend
```bash
cd client
npm install --legacy-peer-deps
```

### ML Service (опционально)
```bash
cd ml-service
pip install -r requirements.txt
```

## Шаг 4: Запуск приложения

### Вариант A: Запуск всего сразу (требуется Docker)
```bash
docker-compose up
```

### Вариант B: Запуск по отдельности

**Backend (FastAPI):**
```bash
cd server
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

**Frontend (React):**
```bash
cd client
npm run dev
```

**ML Service (опционально):**
```bash
cd ml-service
uvicorn app.main:app --reload --host 0.0.0.0 --port 8001
```

## Шаг 5: Проверка работы

- Frontend: http://localhost:5173
- Backend API: http://localhost:8000/docs
- ML Service API: http://localhost:8001/docs

## Проблемы и решения

### "Connection timeout" при подключении к БД

**Причина**: Блокировка IPv6 на вашей системе

**Решение**:
1. Используйте SQL Editor в Supabase (Вариант A выше)
2. Или установите VPN с поддержкой IPv6 (Cloudflare WARP, например)

### "Tenant or user not found"

**Причина**: Неправильный формат connection string

**Решение**: Connection string уже исправлен в `.env` файлах. Если проблема осталась, используйте SQL Editor.

### "npm install" выдает ошибки peer dependencies

**Решение**:
```bash
npm install --legacy-peer-deps --force
```

## Структура проекта

```
89/
├── client/          # React фронтенд (Vite + TypeScript)
├── server/          # FastAPI бэкенд
├── ml-service/      # ML сервис (опционально)
├── schema.sql       # SQL схема базы данных
├── .env             # Главный файл переменных окружения
└── docker-compose.yml
```

## Важные файлы

- `.env` - переменные окружения (DATABASE_URL, API ключи)
- `server/.env` - переменные окружения бэкенда
- `schema.sql` - полная схема базы данных

## Следующие шаги после запуска

1. Зарегистрируйте аккаунт через фронтенд
2. Протестируйте поиск трендов
3. Проверьте AI генерацию скриптов

Готово! 🎉
